export namespace StringManager {
    export enum MenuPage {
        sidesAndDesserts = "SIDES & DESSERTS",
        targetItem = "4 Dipping Sauces",
        totalPrice = "$2.00"
    }

    export enum OrderConfirmationPage {
        confirmationMessage = `ORDER RECEIVED,`
    }
}